[project]
name = "anti-procrastination-backend"
version = "0.1.0"
description = "FastAPI backend for anti-procrastination demo"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "openai>=1.30",
    "google-genai>=0.3",
]

[tool.uv]
package = false
